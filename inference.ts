function showLog(message: any){
    console.log(message);
}

showLog("Hello Word!");
showLog(139);
/**
 * string
 * number
 * boolean
 * arrays
 * Classe
 */

 let test: Array<string> = ['Hello','Test'];
 let isEnabled: boolean = true;
 let test2: [string] = ['xcs'];
 showLog(isEnabled);